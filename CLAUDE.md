<!-- non-dev-harness:v1 -->
# GO 취업

개인용 한국어 취업 지원 웹앱. 저장한 프로필을 기반으로 AI가 이력서 초안을 생성하고, 기업 공고를 분석해 직무 적합도를 파악해주는 도구.

## 프로젝트 정보
- 목적: AI 기반 이력서 생성 및 채용 공고 직무 매칭
- 대상: 개인 (본인 전용)
- 기술: 브라우저 (단일 HTML 파일, Anthropic API)
- 언어: 한국어

## 하네스 파일 안내

이 프로젝트는 5개 파일로 컨텍스트를 관리합니다.

| 파일 | 용도 | 갱신 시점 | 상한 |
|------|------|-----------|------|
| CLAUDE.md | 프로젝트 규칙 (이 파일, 자동 로드) | 규칙 변경 시 | 100줄 |
| MEMORY.md | 의사결정, 작업 이력, 다음 할 일 | /co 체크아웃 | 세션 로그 5개 |
| ERROR.md | 에러 발생→해결 이력 | 에러 해결 시 | 미해결 10개 |
| ISSUE.md | 미해결 이슈, 버그, 요청 | 이슈 발견/해결 | 미해결 15개 |
| WIKI.md | 프로젝트 영구 지식 | 중요 학습 발생 | 제한 없음 |

## 세션 워크플로우

```
세션 시작 → /ci (체크인: 맥락 로드)
    ↓
  작업 진행
    ↓
세션 종료 → /co (체크아웃: 맥락 저장 + 백업)
```

- 세션 시작 시 반드시 /ci 로 이전 맥락을 로드하세요
- 세션 종료 시 반드시 /co 로 작업 내용을 저장하세요
- 파일 수정 전 사용자에게 확인을 받으세요

## 토큰 절약 규칙

1. 큰 파일은 offset/limit으로 필요한 부분만 읽기
2. 같은 파일 중복 읽기 금지
3. git diff는 --stat 우선, 전체 diff는 필요 시만
4. 로그는 최근 3-5개만
5. 응답은 핵심만 간결하게
6. 컨텍스트 65% 도달 시 /compact 안내 (힌트 포함)
7. [중요] **Plan Mode 진입 절대 금지**: 거대한 계획 수립으로 인한 1M 컨텍스트 초과 버그를 방지하기 위해, 파일 생성/수정 요청 시 사전에 거창한 전체 분석이나 Plan Mode를 실행하지 말고 **지정된 단일 파일 작성/수정에 즉시 착수**하십시오.
8. [중요] 기획 단계 템플릿(질문지 서식)이나 가이드라인을 처음부터 다시 재생하지 마십시오. 이미 하네스 세팅 및 기획이 완료되었으므로, 이전 세션의 상태를 로컬 파일로 우선 확인하고 본론 코딩으로 직진하십시오.
9. [중요] **세션 종료 시 산출물 자동 반영 및 백업 필수**: 모든 기능 구현, 버그 수정 등의 '작업이 끝날 때마다' AI는 임의로 세션을 종료해서는 안 되며, 반드시 다음 단계를 순서대로 수행하여 문서를 자동 백업해야 합니다.
   - **1단계 (하네스 최신화)**: 변경되거나 추가된 코드 내역을 바탕으로 `MEMORY.md`(작업 이력 및 다음 할 일), `WIKI.md`(새로 습득한 지식)를 누락 없이 갱신하십시오.
   - **2단계 (자동 백업 트리거)**: 문서 갱신이 완료되면 사용자에게 별도의 안내와 함께 반드시 `/co` (체크아웃 및 백업 명령어)를 실행하여 로컬 환경과 백업 저장소에 작업 내용이 최종 반영되도록 하십시오. 단 하나의 문서나 코드 수정이라도 백업 없이 세션을 이탈하는 것은 절대 금지합니다.
## 프로젝트 규칙

<!-- 프로젝트 진행하며 자동으로 채워집니다 -->
보안 규칙: .env, .env.local, .env.production 파일의 실제 값은 읽거나 출력하지 마세요. 환경변수 이름만 사용하세요.

## 변경이력

| 날짜 | 변경 | 대상 | 이유 |
|------|------|------|------|
| 2026-06-22 | 하네스 초기 세팅 | 전체 | 프로젝트 시작 |
| 2026-06-23 | 기존 자소서·이력서 업로드(PDF/Word) 및 문체 참고 생성 | index.html, api/ | 본인 문서 기반 맞춤 초안 |
| 2026-06-23 | 프로필 탭 UX 개편: 업로드→AI분석→검토 흐름, 드래그앤드롭 | index.html, api/analyze-profile.js | 입력 전 업로드로 사용성 개선 |
| 2026-06-23 | 로컬 API 테스트 설정 (vercel dev, ESM) | package.json, .env.local, README.md | 배포 전 로컬 검증 가능하게 |
| 2026-06-23 | 서비스 기획 재정의 + P1~P3 로드맵 (코드 변경 없음) | WIKI.md, ISSUE.md | 1회성 도구→재사용 루프 전환 |
| 2026-06-23 | P1 구현: 직무별 프로필(goData)·프로젝트 카드·AI 근거·AI→카드 추출 + 보기화면 UX | index.html, api/analyze-profile.js | 정보구조 재설계 실행 |
| 2026-06-23 | 디자인: 이모지 제거 + 배경 미색 | index.html | 사용자 요청(깔끔한 톤) |
| 2026-06-23 | 공고 입력 확장: URL fetch + 이미지 OCR(Upstage) | api/fetch-job.js, api/ocr-job.js | 크롤링 불가·이미지 공고 대응 |
| 2026-06-23 | 자소서/이력서 앙상블(3초안 병합)+마크다운 제거+결과 캐시 | api/generate-*.js, api/_upstage.js, index.html | 생성 품질 안정화 |
| 2026-06-24 | P2 완료: 지원이력 저장·UX개편(자동전달/로딩/토스트)·공고↔프로젝트 매칭·작성 규칙화·프로필 병합업로드+4섹션·스킬 배열화 | index.html, api/_writing-rules.js, api/_upstage.js, api/analyze-profile.js, api/generate-*.js | 재사용 루프 P2 마무리 |
